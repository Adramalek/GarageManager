let prefix = "http://localhost:8080/api";
let profile_prefix = prefix+'/profile';
let cars_url = prefix+"/cars";
let drivers_url = prefix+"/drivers";
let garage_url = prefix+"/garage";
let cars_profile = profile_prefix+'/cars';
let drivers_profile = profile_prefix+'/drivers';
let garage_profile = profile_prefix+'/garage';

export {prefix, cars_url, drivers_url, garage_url, cars_profile, garage_profile, drivers_profile, profile_prefix}